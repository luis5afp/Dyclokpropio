'use strict';

/**
 * Compatibility adapter around the recovered Electron main-process bundle.
 *
 * New code should import this module rather than reaching into the generated
 * dist-electron filename directly. When functionality is reconstructed, the
 * corresponding export can be replaced here without changing callers.
 */
module.exports = require('../../dist-electron/main-CuCOh59n.js');
